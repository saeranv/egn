import numpy as np
import matplotlib.pyplot as plt
import viz.plt as vlt


f, ax = vlt.subplots(2,2)
print(ax, len(ax))
print(f)
# ax.scatter(np.random.rand(100), np.random.rand(100))
# vlt.stream_plt(f)

